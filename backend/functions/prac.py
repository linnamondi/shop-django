# Counting occurences  alist
def occurences(days):
    count=0
    for day in days:
        if day=='Monday':
            count+=1
        return count

def numOccurence(numbers):
    count=0
    for num in numbers:
        if num == 1:
            count+=1
    return count

def checkString(text):
    for x in text:
     if "banana" in text:
        return True
    else:
        return False

 # range
def myNum(numbers):

    for i in range(len(numbers)):

        if i <5:
            numbers[i]*8
        else:
            numbers[i]+5
            print(numbers)

def get_range(numbs):
    for i in range(100,2001):
            if i%7 !=0:
                print(numb)
    
def freq(number_list):
    count=1
    frequency_dict={}
    for number in number_list:
        if number in number_list:
            frequency_dict[number] +=count
    else : 
        frequency_dict[number]=count

               
        frequency_dict()
    



                

            
            



